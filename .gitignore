.gradle
build
alt-home/
.idea/
.project
.settings/
.classpath
nbactions.xml
nb-configuration.xml
nbproject/
# Local-only, never in the public mirror:
# machine-specific Gradle JDK path (breaks any other machine, incl. hub CI)
gradle.properties
# Perforce working config
.p4config
.p4ignore
# dev tooling and local helpers - not needed to build or run the plugin
src/test/
store-tests/
test-store.cmd
run.cmd
run-alt.cmd
# docs kept to the essentials: builder, store script, example + test boards
docs/clan-logo-bingo-full.png
docs/progression-board-8x8.json
